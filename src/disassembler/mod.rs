pub fn disassemble(bin_file_name: &str, asm_file_name: &str) -> Result<(), ()> {
    // Open the input and output file
    // let bin_file = File::options().read(true).open(bin_file_name)?;
    // let asm_file = File::options().write(true).create(true).open(asm_file_name)?;

    Ok(())
}